import type { ProjectViewModel } from "./types";

export const getProjectStaticParamsSlugs = async () => {
	return [];
};

export const getProjects = async (): Promise<ProjectViewModel[]> => {
	return [];
};

export const getProjectBySlug = async (_slug: string): Promise<ProjectViewModel | null> => {
	return null;
};
